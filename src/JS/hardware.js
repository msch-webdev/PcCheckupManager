'use strict';const si=require('systeminformation');const hddSpace=require('hdd-space');const hardwareBtn=document.getElementById('hardwareBtn');hardwareBtn.addEventListener('click',()=>{si.osInfo().then(data=>{console.log('OS => ',data);const betriebsSystem=document.getElementById('os');const builderWin=['21H1','20H2','2004','1909'];let update='';let platt=data.platform;let distr=data.distro;let archi=data.arch;let buildVersion=data.build;if(buildVersion==19043){update=builderWin[0];}else if(buildVersion==19042){update=builderWin[1];}else if(buildVersion==19041){update=builderWin[2];}else if(buildVersion==18363){update=builderWin[3];}else{update='n.a.'} let serialNr=data.serial;betriebsSystem.innerHTML=`<div class="os"><div>-System:${platt}</div><div>-OS:${distr}/${archi}Bit</div><div>-Update Version:${buildVersion}(${update})</div><div>-Serien Nr:${serialNr}</div></div>`;});si.baseboard().then(data=>{console.log('Motherboard =>',data);const motherBoard=document.getElementById('motherBoard');let manufacturer=data.manufacturer;let model=data.model;let vers=data.version;si.bios().then(data=>{console.log('Bios =>',data); let bios=data.version;let release=data.releaseDate;motherBoard.innerHTML=`<div class="motherBoard"><div>-Hersteller:${manufacturer}</div><div>-Model:${model}</div><div>-Version:${vers}</div><div>-Bios:${bios}/Release:${release}</div</div>`;});});si.cpu().then(data=>{console.log('CPU => ',data);const cpu=document.getElementById('cpu');let manufacturer=data.manufacturer;let brand=data.brand;let speed=data.speed;let cores=data.cores;cpu.innerHTML=`<div class="cpu"><div>-Hersteller:${manufacturer}</div><div>-Model:${brand}</div><div>-Cores:${cores}x ${speed}GHz</div</div>`;}); si.mem().then(data=>{console.log('Memory 1 => ',data);let totalMemGb=Math.ceil((data.total/(1024*1024*1024)).toFixed(2));si.memLayout().then(data=>{console.log('Memory 2 => ',data);const memoryType=document.getElementById('ram');let type=data[0].type;memoryType.innerHTML=`<div>-Grösse:${totalMemGb}GB ${type}</div>`;});});si.graphics().then(data=>{console.log('Grafikkarten => ',data);const grafik=document.getElementById('grafik');for(const karte of data.controllers){let model=karte.model;let vram=karte.vram;grafik.innerHTML+=`<div class="grafik"><div>-Model:${model}</div><div>-Ram:${vram}</div</div>`;}});si.diskLayout().then(data=>{console.log('Laufwerk => ',data);const laufwerkeName=document.getElementById('laufwerkeName');for(let i=0;i<data.length;i++){if(data[i].interfaceType!=='USB'){let name=data[i].name;let type="";let interfaceInfo=data[i].interfaceType;if(data[i].type!=='SSD'){type="HDD";}else{type="SSD";} laufwerkeName.innerHTML+=`<div class="hardDisc"><div>-Name:${name}</div><div class="tab">-Type:${type}</div><div class="tab">-Interface:${interfaceInfo}</div</div>`;}}});hddSpace({format:'auto'},function(info){console.log('Laufwerksspeicher => ',info);const laufwerke=document.getElementById('laufwerke');let letter='';let free='';let size='';for(let i=0;i<info.parts.length;i++){let usb=parseFloat(info.parts[i].size);if(usb>5){letter=info.parts[i].letter;free=info.parts[i].free;size=info.parts[i].size;laufwerke.innerHTML+=`<div>-<span>${letter}</span><span>${free}</span>frei von<span>${size}</span></div>`;}};});},{once:true});